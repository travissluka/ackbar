# The variational solver: 3DVar as it stands, and the base every other
# variational experiment inherits from. An EnVar adds `da/envar`, a hybrid adds
# `da/hybrid`, and FGAT or 4DEnVar change `solver.window.type`. Nothing else
# about this file changes with any of them.
#
# It carries the background error and the minimizer, which are the only two
# parts of the analysis document ACKBAR cannot derive from something the
# experiment already states. Everything else soca_var.x reads is built: the
# geometry from the model layer, the background from the previous cycle's
# restarts, the window from the cycle, the observers from `observations`.
#
# **The standard deviations below are two stated decisions and a set of
# defaults.** The decisions are that the mixed layer temperature error comes
# from the GODAS field rather than one number for the whole domain, and that
# unbalanced sea surface height error is zero so that height moves only when
# water does. Everything else is `SOCAParametricOceanStdDev`'s own default and
# is not known to be right for anywhere. That block is the thing to read before
# believing a result, and `docs/background-error.md` says what it takes.
#
# `tools/soca-dirac.sh <domain> --full` applies exactly what is written here,
# standard deviations and balance included, to a handful of point sources and
# reports what came out. It is the cheapest check that a change to this file did
# what it was meant to.
#
# What to reach for first, in order: `ninner` if the minimization is not
# converging or is spending wall clock after it has, the standard deviations if
# the increment is the wrong size, and the diffusion lengths, which are not here
# because they are calibrated offline per domain.

solver:
  name: variational
  covariance: static
  window:
    type: '3d'

  # Ocean only, matching the model layer's `state variables`. The ice fields in
  # the bundle's example are absent for the same reason they are absent there:
  # SIS2 writes `ice_model.res.nc` and the fields metadata's ice section is
  # CICE's, so an ice variable here resolves to something that does not exist.
  # The first ice observer is what makes that worth solving.
  analysis variables:
  - sea_water_potential_temperature
  - sea_water_salinity
  - sea_surface_height_above_geoid

  # The three above plus what the background error blocks read and never write.
  # `sea_water_cell_thickness` is what makes anything addressable by depth;
  # `ocean_mixed_layer_thickness` and `sea_water_depth` are what
  # SOCAParametricOceanStdDev builds its standard deviations from. All three are
  # derived fields in `config/model/mom6sis2/fields_metadata.yaml` with no `io
  # name`, so they cost nothing to ask for.
  background variables:
  - sea_water_potential_temperature
  - sea_water_salinity
  - sea_surface_height_above_geoid
  - sea_water_cell_thickness
  - ocean_mixed_layer_thickness
  - sea_water_depth

  background error:
    covariance model: SABER

    # Correlation, as a diffusion operator. The three filepaths are the product
    # of `tools/soca-diffusion.sh`, which is offline and keyed on domain the way
    # the gridspec is. Until it has run, `ackbar validate` step 3 names them as
    # missing, which is the intended failure: a B calibrated on a different grid
    # than the model integrates on is what this arrangement exists to make
    # impossible. The lengths those files encode are in
    # `config/static/diffusion.yaml`, which nothing here reads, for the same
    # reason nothing here restates the gridgen configuration.
    #
    # `filepath` is a stem rather than a filename. saber appends `.nc`.
    #
    # The vertical scales are calibrated once, against the mixed layer of one
    # restart, rather than recalibrated every cycle. Per-cycle is the measured
    # improvement to make later; what makes it optional is that MLD moves slowly
    # compared to a DA cycle.
    saber central block:
      saber block name: diffusion
      read:
        groups:
        - variables:
          - sea_water_potential_temperature
          - sea_water_salinity
          horizontal:
            filepath: $(domain_static)/diffusion/corr_hz
          vertical:
            levels: $(diffusion_levels)
            filepath: $(domain_static)/diffusion/corr_vt
            # Implicit in the vertical, explicit (the default) in the
            # horizontal. The explicit scheme's iteration count grows with the
            # square of the scale in grid units, and a mixed layer can span a
            # large number of levels; the implicit scheme is a fixed count of
            # tridiagonal solves down each column instead, which is also what
            # lets the vertical scales go uncapped. Horizontally the scales are
            # a few cells and explicit is cheaper.
            #
            # These two values must match `vertical:` in `config/static/diffusion.yaml`.
            # The normalization in `corr_vt.nc` was computed with the operator that
            # file configures, and reading it back through a differently
            # configured operator produces a B that is wrong by a factor nothing
            # reports. `tests/test_diffusion.py` is what holds them together.
            method: implicit
            iterations: 2
        - variables: [sea_surface_height_above_geoid]
          horizontal:
            filepath: $(domain_static)/diffusion/corr_hz_ssh

    saber outer blocks:
    # Taper the background error with depth, so the analysis does not push the
    # same amplitude into the abyss that it does into the thermocline.
    #
    # **`ocean_depth_min` is a cutoff, not a taper, and the name is
    # misleading.** `BkgErrFilt.cc` reads
    #
    #     if (depth <= oceanDepthMin) continue;
    #
    # which leaves the multiplier at zero, so in water shallower than this the
    # background error is not reduced, it is **deleted**. At the 1000 m the
    # bundle's example uses, that removes the entire continental shelf: on the
    # Gulf domains the Louisiana-Texas shelf, the West Florida shelf and
    # Campeche Bank, which is most of the domain's area and exactly where the
    # SST observations are densest. Those observations are then read, evaluated,
    # and multiplied by an error of zero. Hence 0 here.
    #
    # What the cutoff was for is real, an ssh increment over a shelf is not
    # meaningful, and a hard threshold on total depth is a blunt way to say so.
    # Something better belongs here eventually and belongs per variable.
    #
    # `efold_z` is the part that is a taper.
    - saber block name: SOCABkgErrFilt
      ocean_depth_min: 0      # [m]
      rescale_bkgerr: 1.0
      efold_z: 2500.0         # [m]

    # The standard deviations. `ParametricOceanStdDev.cc` builds the temperature
    # one from the background's own vertical gradient,
    #
    #     stddev   = clamp(dz * |dT/dz|, localMin, max)
    #     localMin = min + (sst - min) * exp(-depth / efold)
    #
    # which is the right shape through a thermocline and is **zero inside a
    # mixed layer**, where dT/dz is zero by definition. So `sst` below is not a
    # cosmetic floor: it is the entire mixed layer temperature error, and it is
    # what every SST observation is weighed against.
    #
    # `min`, `max`, `dz` and `efold` are the block's defaults (0.1 K, 2.0 K,
    # 20 m, 500 m), which are also what hat10 states explicitly.
    #
    # **These are good enough and are not tuned.** What they are measured
    # against is `tools/soca-dirac.sh <domain> --full`, whose `save
    # diagnostics` writes the standard deviations, beside the free run's own
    # error against the nature run on the state space page. A cycling
    # background is better than a free run, so that error is an upper bound on
    # what B should be, and the test applied here was only that nothing sits
    # above its own upper bound.
    - saber block name: SOCAParametricOceanStdDev
      temperature:
        # **The near surface layers are excluded from dT/dz, and this is the one
        # number here that had to change.** The default is a centimetre, which
        # assumes a grid whose top layers are much thicker than this one's two
        # metres. With it, the gradient at the top two levels is measured across
        # 1 to 5 m of a background stamped at 00Z, which is late afternoon at
        # 90 W: it reads the diurnal warm layer, `dz * |dT/dz|` comes out at
        # 4 K, and the surface standard deviation is the 2.0 K ceiling over three
        # fifths of the basin. That is six times the free run's own surface
        # error, which no background can be worse than.
        #
        # At 3 m the mask reaches the level near 20 m where the spacing starts to
        # grow, so the sst floor governs the mixed layer, which is what it is for.
        # The surface falls from 2.00 K to 0.38 K and every level from 50 m down
        # moves by less than two per cent. 5 m gives nearly the same answer, so
        # the exact value is not delicate.
        #
        # It is the same trap `ackbar/diffusion.py` documents for MOM6's
        # instantaneous `MLD`, reached from the other side.
        min layer thickness: 3.0

        # The GODAS field, remapped onto this domain's cell centres by
        # `tools/sst-bgerr.py`. It replaces a flat 1.0 K that was a placeholder
        # for want of the file, and that was about two and a half times what
        # this field says the Gulf is: 0.20 to 0.92 K, low in the western basin
        # interior and on Campeche Bank, high along the Loop Current and the
        # Florida Current.
        #
        # A full filename and not a stem. The diffusion block above takes stems
        # because saber appends `.nc` to those; `readNcAndInterp` does not.
        sst:
          filepath: $(domain_static)/sst_bgerr.nc
          variable: sst_bgerr

      # Shaped by the mixed layer, at twice the block's default ceiling. The
      # deep value is a tenth of `max` by construction, so 0.25 put 0.025 psu
      # everywhere below a hundred metres against a free run error of 0.12 to
      # 0.25, and 0.22 at the surface against 0.70. Doubling it is closer
      # everywhere and still under the upper bound everywhere, which is as far
      # as one restart's diagnostics can honestly take it.
      #
      # It matters more here than it would elsewhere because `kst` is off: with
      # no temperature to salinity balance, salinity moves only where a
      # salinity observation is, and this is what decides how much it moves.
      unbalanced salinity:
        max: 0.5

      # **Zero, so that every sea surface height increment arrives through the
      # balance operator below**, out of the temperature and salinity the
      # analysis actually corrected. The block's default is 0.1 m everywhere,
      # and that is an *unbalanced* height increment: it moves the free surface
      # without moving any water, and the model spends the first hours of the
      # forecast reconciling the two. hat10 sets this to zero for the same
      # reason. It is also what makes the ssh column of a dirac test through
      # this file meaningful, since a response there is then entirely balance.
      #
      # A var rather than a literal because it is the one number in this block
      # whose right value is an open question rather than a default: it decides
      # how much of an altimeter's increment the balance operator has to carry,
      # and with `kst` off the balance operator carries it entirely in
      # temperature. `docs/background-error.md` has the measurement.
      unbalanced ssh:
        min: $(unbalanced_ssh)
        max: $(unbalanced_ssh)

    # The balance operator, which is what makes an increment in temperature
    # imply one in sea surface height rather than two independent analyses of
    # the same water. `ksshts` is the T and S to height jacobian and it is on.
    #
    # **`kst`, the temperature to salinity jacobian, is absent and that means
    # off, not defaulted.** `soca_balance_mod.F90` initialises the jacobian to
    # zero and fills it only `if ( f_conf%has("kst") )`, and the four keys
    # inside are all `get_or_die`, so there is no default to fall back to: an
    # analysis through this file puts no salinity anywhere it saw only
    # temperature. `tools/soca-dirac.sh gom_25km --full` reports exactly 0.0000
    # psu against a 1.5 degC temperature response, which is that.
    #
    # Left off for now rather than guessed at, because the four numbers are a
    # local water mass statement (`dsdtmax`, `dsdzmin`, `dtdzmin`, `nlayers`)
    # and the Gulf's subtropical underwater maximum sits above its own
    # temperature inversion in places, where a T to S regression has the wrong
    # sign. hat10 carries them commented out for what is probably the same
    # reason. It matters most where the network has temperature and no salinity.
    #
    # `input variables` and `output variables` belong here and are absent on
    # purpose: both are the analysis variables, which are already stated once
    # above, and `ackbar/soca.py` fills them in. They are not optional. Without
    # `input variables`, `oops::ModelSpaceCovarianceBase` holds a null pointer
    # that it dereferences the first time it evaluates Jb, which is after every
    # block has been built and reported.
    linear variable change:
      linear variable changes:
      - linear variable change name: BalanceSOCA
        ksshts:
          nlayers: 2

  variational:
    # The reduction target is what should stop the minimization; `ninner` is a
    # backstop it rarely reaches. See `docs/analysis.md` for the measurement
    # behind these two numbers, and `tools/local/var-convergence.py` for
    # checking which one a given cycle actually stopped on.
    minimizer:
      algorithm: RPCG
    iterations:
    - ninner: $(ninner)
      # Written with the dot and the signed exponent, not `1e-3`: PyYAML reads
      # YAML 1.1 and would hand eckit the string. See ackbar.config.lint, which
      # rejects the other spelling.
      gradient norm reduction: 1.0e-3

vars:
  # `diffusion_levels` is the domain's, declared in the domain layer, because it
  # is that domain's NK.

  # The unbalanced sea surface height standard deviation, in metres. Zero, for
  # the reason stated beside the key itself: every height increment then arrives
  # through the balance operator, out of water the analysis actually moved.
  unbalanced_ssh: 0.0

  # Inner iterations per outer loop. One outer loop, which is what `3d` means
  # here; FGAT and 4D add loops rather than iterations.
  #
  # A backstop against a solve that never reaches the reduction target above,
  # not the number expected to bind. `docs/analysis.md` has the measurement
  # behind the value.
  ninner: 100
